import { IonHeader, IonPage, IonContent } from '@ionic/react';
import React from 'react';

const AcessDenied: React.FC = () => {
    return (
      <IonPage>
        <IonHeader>NEGADO</IonHeader>
      </IonPage>
    );
};

export default AcessDenied;
